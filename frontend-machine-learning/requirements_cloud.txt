# Streamlit app requirements - optimized for Python 3.13 and cloud deployment
streamlit>=1.40.0

# Core data processing - using versions with precompiled wheels for Python 3.13
pandas>=2.1.0
numpy>=1.24.0,<2.0.0

# Machine learning - using compatible versions
scikit-learn>=1.3.0
joblib>=1.3.0

# Scientific computing - letting pip find the best compatible version
scipy>=1.10.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# NLP and text processing
nltk>=3.8.0

# Optional: Only include if actually needed for recommendations
# gensim>=4.2.0

# Utilities
tqdm>=4.65.0

# Only include web dependencies if actually used
# fastapi>=0.100.0
# uvicorn[standard]>=0.23.0
# pydantic>=2.0.0

# Only include if Spotify integration is actually used
# spotipy>=2.22.0

# HTTP requests and environment
requests>=2.31.0
python-dotenv>=1.0.0

# Only include if needed for file uploads
# python-multipart>=0.0.6
